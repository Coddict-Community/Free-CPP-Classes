#include <iostream>
using namespace std;

int main()
{
    int a, b, result;

    cout << "Enter the First Number: ";
    cin >> a;
    cout << "Enter the Second Number: ";
    cin >> b;

    // result = a + b;
    // result = a - b;
    // result = a * b;
    // result = a / b;
    cout << "Result = " << result;
    return 0;
}
